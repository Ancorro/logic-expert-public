"""Core implementation modules for logic-stream training and inference.

This subpackage contains routing/gating layers, no-gate controls, model wrappers,
fusion/projection blocks, and dataset utilities used by notebook and training
pipelines.
"""
